{
    "components": {
        "parameters": {
            "page[offset]": {
                "in": "query",
                "name": "page[offset]",
                "required": false,
                "description": "Sets offset for list of items",
                "schema": {
                    "type": "integer",
                    "minimum": 0
                },
                "example": 20
            },
            "page[limit]": {
                "in": "query",
                "name": "page[limit]",
                "required": false,
                "description": "Limits the number of returned items",
                "schema": {
                    "type": "integer",
                    "minimum": 1
                },
                "example": 10
            }
        }
    }
}
